diff --git a/node_modules/hardhat-deploy/dist/src/DeploymentFactory.js b/node_modules/hardhat-deploy/dist/src/DeploymentFactory.js
index d267c3b..a263e96 100644
--- a/node_modules/hardhat-deploy/dist/src/DeploymentFactory.js
+++ b/node_modules/hardhat-deploy/dist/src/DeploymentFactory.js
@@ -58,10 +58,18 @@ class DeploymentFactory {
     async getDeployTransaction() {
         let overrides = this.overrides;
         if (this.isZkSync) {
+            const params = zk.utils.getPaymasterParams(
+                "0x950e3Bb8C6bab20b56a70550EC037E22032A413e", // Paymaster address
+                {
+                  type: "General",
+                  innerInput: new Uint8Array(),
+                }
+              );
             const factoryDeps = await this.extractFactoryDeps(this.artifact);
             const customData = {
                 customData: {
                     factoryDeps,
+                    paymasterParams: params,
                     feeToken: zk.utils.ETH_ADDRESS,
                 },
             };
diff --git a/node_modules/hardhat-deploy/dist/src/helpers.js b/node_modules/hardhat-deploy/dist/src/helpers.js
index 7f646de..8485514 100644
--- a/node_modules/hardhat-deploy/dist/src/helpers.js
+++ b/node_modules/hardhat-deploy/dist/src/helpers.js
@@ -398,6 +398,7 @@ getArtifact, saveDeployment, willSaveToDisk, onPendingTx, getGasPrice, log, prin
         await init();
         const deployFunction = () => deploy(name, Object.assign(Object.assign({}, options), { deterministicDeployment: options.salt || true }));
         if (options.proxy) {
+            console.log("deploying contract 4");
             /* eslint-disable prefer-const */
             let { viaAdminContract, proxyAdminDeployed, proxyAdminName, proxyAdminContract, owner, proxyAdmin, currentProxyAdminOwner, artifact, implementationArgs, implementationName, implementationOptions, proxyName, proxyContract, proxyArgsTemplate, mergedABI, updateMethod, updateArgs, } = await _getProxyInfo(name, options);
             /* eslint-enable prefer-const */
diff --git a/node_modules/hardhat-deploy/src/helpers.ts b/node_modules/hardhat-deploy/src/helpers.ts
index d85d706..8cc2ea2 100644
--- a/node_modules/hardhat-deploy/src/helpers.ts
+++ b/node_modules/hardhat-deploy/src/helpers.ts
@@ -2437,6 +2437,7 @@ Note that in this case, the contract deployment will not behave the same if depl
     name: string,
     options: DeployOptions
   ): Promise<DeployResult> {
+    console.log("deploying contract 5");
     options = {...options}; // ensure no change
     await init();
     if (!options.proxy) {
